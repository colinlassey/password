import crypto from 'crypto';

const encrypt = (user, pass) => {
    // figure out how to encrypt usernames and passwords to be stored for logins
    // only need usernames/passwords to be decrypted for the purpose of logging in

}
